# Optional: clone libtorrent here to avoid auto-download on cmake configure.
#
#   git clone --depth 1 --branch v2.0.11 https://github.com/arvidn/libtorrent.git thirdparty/libtorrent
#
# If this folder is empty, CMake fetches libtorrent + Boost automatically.
