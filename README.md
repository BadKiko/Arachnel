# Arachnel

Qt 6 + QmlMaterial (Material 3), шаблон как `voxbridge_ui`.

## Запуск

```bash
./run.sh --rebuild
```

## Сборка вручную

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
./build/arachnel_app
```
